<Window x:Class="Viper.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Viper — App Locked" Height="400" Width="480"
        WindowStartupLocation="CenterScreen"
        Topmost="True" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#0A0A0A"/>
            <Setter Property="FontFamily" Value="Inter, Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontFamily" Value="Inter, Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A1A1A"/>
                                <Setter Property="Foreground" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer container with subtle shadow effect -->
    <Border Background="#0A0A0A" CornerRadius="8" BorderBrush="#1A1A1A" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.8" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header area -->
            <StackPanel Grid.Row="0" Margin="32,32,32,0">
                <!-- Lock icon -->
                <TextBlock Text="🔒" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                
                <!-- App name -->
                <TextBlock x:Name="TitleText" Text="Firefox is locked" 
                           Foreground="#FFFFFF" FontSize="22" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,6"
                           FontFamily="Inter, Segoe UI"/>

                <!-- Subtitle -->
                <TextBlock x:Name="SubtitleText" Text="Enter your App Unlock Password to continue" 
                           Foreground="#666666" FontSize="12" 
                           HorizontalAlignment="Center" Margin="0,0,0,0"
                           FontFamily="Inter, Segoe UI"/>
            </StackPanel>

            <!-- Password input area -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="40,20,40,0">
                <!-- Password field with border -->
                <Border Background="#111111" CornerRadius="4" BorderBrush="#222222" BorderThickness="1"
                        Margin="0,0,0,8">
                    <PasswordBox x:Name="PasswordInput" 
                                 Height="44" FontSize="16" 
                                 Background="Transparent" Foreground="#FFFFFF" 
                                 BorderThickness="0"
                                 FontFamily="JetBrains Mono, Consolas, Courier New"
                                 CaretBrush="#FFFFFF"
                                 Padding="12,0"/>
                </Border>

                <!-- Error text -->
                <TextBlock x:Name="ErrorText" Text="" 
                           Foreground="#CC4444" FontSize="12" 
                           HorizontalAlignment="Center" Margin="0,0,0,4"
                           FontFamily="Inter, Segoe UI"
                           Visibility="Collapsed"/>

                <!-- Attempt counter (hidden until failures occur) -->
                <TextBlock x:Name="AttemptText" Text="" 
                           Foreground="#555555" FontSize="11" 
                           HorizontalAlignment="Center" Margin="0,4,0,0"
                           FontFamily="Inter, Segoe UI"
                           Visibility="Collapsed"/>
            </StackPanel>

            <!-- Button area -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,28">
                <Button x:Name="CancelBtn" Content="Cancel" 
                        Style="{StaticResource SecondaryButton}" 
                        Margin="0,0,12,0"
                        Click="CancelBtn_Click"/>
                <Button x:Name="UnlockBtn" Content="Unlock" 
                        Style="{StaticResource PrimaryButton}" 
                        Click="UnlockBtn_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
