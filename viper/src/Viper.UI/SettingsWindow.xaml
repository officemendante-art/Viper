<Window x:Class="Viper.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Viper — Settings" Height="500" Width="550"
        WindowStartupLocation="CenterScreen"
        Topmost="True" ResizeMode="NoResize"
        WindowStyle="None" Background="#1E1E1E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="Viper Settings" Foreground="White" FontSize="24" FontWeight="Bold"
                       FontFamily="Inter, Segoe UI"/>
            <TextBlock Text="Manage protected applications" Foreground="#888888" FontSize="12"
                       FontFamily="Inter, Segoe UI" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- App List -->
        <Border Grid.Row="1" Margin="20,0" Background="#2A2A2A" CornerRadius="4">
            <ListBox x:Name="AppList" Background="Transparent" BorderThickness="0"
                     Foreground="White" FontSize="13" FontFamily="JetBrains Mono, Consolas"
                     SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="4">
                            <TextBlock Text="{Binding DisplayName}" Foreground="White" Width="120"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Path}" Foreground="#888888"
                                       VerticalAlignment="Center" FontSize="11"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Controls -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
            <Button x:Name="AddBtn" Content="Add App" Width="100" Height="34"
                    Background="#007ACC" Foreground="White" BorderThickness="0" FontWeight="Bold"
                    Margin="0,0,10,0" Click="AddBtn_Click"
                    FontFamily="Inter, Segoe UI"/>
            <Button x:Name="RemoveBtn" Content="Remove" Width="100" Height="34"
                    Background="#CC3333" Foreground="White" BorderThickness="0" FontWeight="Bold"
                    Margin="0,0,10,0" Click="RemoveBtn_Click"
                    FontFamily="Inter, Segoe UI"/>
            <Button x:Name="CloseBtn" Content="Close" Width="100" Height="34"
                    Background="#555555" Foreground="White" BorderThickness="0" FontWeight="Bold"
                    Click="CloseBtn_Click"
                    FontFamily="Inter, Segoe UI"/>
        </StackPanel>
    </Grid>
</Window>
